<div class="row">
    <div class="col-12 col-md-6">
        <h5 class="mt-5 mb-3" style="color: #0470C1; font-weight:400">🔎 Dans quelle agglomération recherchez-vous ? *</h5>
            <%= form_tag('/step-2', :id => 'zone_selection_form', method: :get) do |f| %>

            <div class="d-flex flex-wrap">
                <div class="card-group">
                <% @agglos_infos.each do |agglo| %>
                <div>

                </div>
                  <div class="card">
                    <img class="card-img-top" src="<%= agglo["image_url"] %>" alt="Card image cap">
                    <div class="card-body">

                    <%= check_box_tag 'selected_zones', 
                        agglo["agglomeration"]
                    %>
                    <span class="form-check-label mb-2 ml-2 box_hover" >
                        <%= agglo["agglomeration"] %>
                    </span>
                    </div>
                  </div>
                <% end %>
            </div>
            <nav class="navbar navbar-light fixed-bottom d-flex justify-content-center bg-blue">
                <div class="d-flex justify-content-center mt-2 mb-2">
                    <%= submit_tag "C'est parti ! 🚀", :name => 'commit', class: "btn btn-outline-light", style:"font-weight:600" %>
                </div>
            </nav>
            <% end %>
    </div>
</div>

<script>
$(document).ready(function(){
    $('input:checkbox').click(function() {
        $('input:checkbox').not(this).prop('checked', false);
    });
});
</script>