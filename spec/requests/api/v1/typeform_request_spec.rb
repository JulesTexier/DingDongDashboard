require "rails_helper"

RSpec.describe "Api::V1::Typeforms", type: :request do
  it "add a comment to the Trello Card" do

    # post "/trello/add_action", :params => { :card_id => "5e96bf48f6cf5d1d109a9a5f"  , :text => "Simple test text" }
    # byebug
    # expect(response).to redirect_to(assigns(:widget))
    # follow_redirect!

    # expect(response).to render_template(:show)
    # expect(response.body).to include("Widget was successfully created.")
  end
end
